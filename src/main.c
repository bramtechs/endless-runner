#include <SDL.h>
#include <stdio.h>
#include "defs.h"

int main(void){

    // create the SDL2 window
    int nRenderFlags = SDL_RENDERER_ACCELERATED;
    int nWindowFlags = 0; 

    printf("Created window of size %d by %d",WIDTH,HEIGHT);
    return 0;
}
